<?xml version="1.0"?>
<ruleset name="LinchpinPHPCS">
	<description>Linchpin WordPress Project PHPCS Sniffs</description>

	<rule ref="WordPress">
		<exclude name="WordPress.Files.FileName.NotHyphenatedLowercase" />
		<exclude name="WordPress.Files.FileName.InvalidClassFileName" />
		<exclude name="Generic.Arrays.DisallowShortArraySyntax.Found" />
	</rule>

	<rule ref="PHPCompatibility"/>
	<config name="testVersion" value="7.3-"/>

	<arg name="extensions" value="php"/>
	<arg value="s"/>

	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/vendor/*</exclude-pattern>
	<exclude-pattern>.github/*</exclude-pattern>
	<exclude-pattern>.deployment/*</exclude-pattern>
	<exclude-pattern>plugins/*</exclude-pattern>
	<exclude-pattern type="relative">^plugins/(?!box-of-scraps/).+</exclude-pattern>
<!-- The following lines are specific to plugin scanning
	 Uncomment these lines if needed in your project
	<exclude-pattern>plugins/box-of-scraps/vendor/*</exclude-pattern>
	<include-pattern>plugins/box-of-scraps/*</include-pattern> -->
	<exclude-pattern>themes/(?!box-of-scraps/)*</exclude-pattern>
</ruleset>
